import 'package:flutter/material.dart';

class UncompletedItems extends StatelessWidget {
  const UncompletedItems({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Uncompleted TASKS"),
    );
  }
}
